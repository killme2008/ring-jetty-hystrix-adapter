(ns ring-jetty-hystrix-adapter.core-test
  (:require [clojure.test :refer :all]
            [ring-jetty-hystrix-adapter.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
